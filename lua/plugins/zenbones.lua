-- Use the Zenbones Theme.
return function(use)
    vim.g.zenbones = {
    }

    use { 'mcchrish/zenbones.nvim',
        requires = 'rktjmp/lush.nvim',
        config   = function()
        end
    }
end

